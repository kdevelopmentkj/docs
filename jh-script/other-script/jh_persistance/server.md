# Server

***

```lua
exports.getGlobalFunctions()
```

*   **return** all function

    ```lua
    add(entityNetId)
    ```

    * entityNetId: `number`
    * return vim string, newEntityId `number`



    ```lua
    remove(entityNetId)
    ```

    * entityNetId: `number`



    ```lua
    changeEnable(vim, enable)
    ```

    * vim: `string`
    * enable: `boolean`



    ```lua
    getVehPersistent()
    ```

    * **return** the list with all entityIds



    ```lua
    isPersistent(entityNetId)
    ```

    * entityNetId: `string`
    * **return** `boolean`

